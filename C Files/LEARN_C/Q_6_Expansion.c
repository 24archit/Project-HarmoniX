#include<stdio.h>
int main() {
    float a;
    float b;
    float sum =0;
    printf("Enter the value of x: ");
    scanf("%f", &a);
    printf("Enter the accuracy for number of terms: ");
    scanf("%f", &b);
    for(int i=1; i<=a; )    
    return 0;
}